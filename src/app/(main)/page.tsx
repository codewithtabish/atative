import { Container } from "@/components/(app)/(common)/layout/container";
import HomeBlog from "@/components/(app)/(pages)/homepage/home-page-list";

const HomeScreen = () => {
  return (
    <Container>
      <HomeBlog />
    </Container>
  );
};

export default HomeScreen;
